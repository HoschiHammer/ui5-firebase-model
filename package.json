{
  "name": "ui5-firebase-model",
  "description": "Experimental: An OpenUI5 model which uses a firebase realtime database as its backend",
  "version": "0.1.2",
  "repository": {
    "type": "git",
    "url": "https://github.com/jumpifzero/ui5-firebase-model"
  },
  "license": "Apache-2.0",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified. Please add some.\" && exit 1",
    "start": "grunt"
  },
  "devDependencies": {
    "firebase-server": "^0.11.0"
  },
  "dependencies": {
    "npm": "^5.4.0"
  }
}
